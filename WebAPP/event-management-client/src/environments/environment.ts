export const environment = {
  production: false,
  apiUrl: 'https://localhost:44388', // Update this to match your API URL
  features: {
    eventsByType: false, // Set to true when backend implements this endpoint
    subEvents: false,     // Set to true when backend implements this endpoint
    fileUpload: false
  }
};